---
description: 스킬의 트리거 정확도와 품질 델타를 측정한다
---

`skill-eval:score` 스킬을 사용해 `$ARGUMENTS`로 지정된 스킬을 평가하라.

인자가 없으면 eval 홈(soohan-skills 체크아웃의 `evals/` 또는 `~/.skill-eval/evals/`) 아래에
케이스가 있는 스킬 목록을 보여주고 어느 것을 평가할지 물어라.
